import React, { useMemo, useState } from 'react';
import { AlertTriangle, ArrowRight, BadgeCheck, Bot, Building2, Camera, CheckCircle2, Database, Download, Droplets, FileCheck2, FileText, Gauge, Layers3, Leaf, Lock, MapPin, Network, Plus, QrCode, ScanLine, ShieldAlert, ShieldCheck, Sparkles, Trees, Upload, Zap } from 'lucide-react';
import { Area, AreaChart, Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { demoSnapshot, selectCeoDashboard, selectMapletreePilotNarrative, METHOD_VERSION, demoAssets, type AssetType, type EvidenceLevel, type HealthCondition, type ReportingRight } from './domain/engine';

const timelineData = [{ month: 'Jan', carbon: 0.11, readiness: 54 }, { month: 'Feb', carbon: 0.13, readiness: 60 }, { month: 'Mar', carbon: 0.15, readiness: 67 }, { month: 'Apr', carbon: 0.16, readiness: 73 }, { month: 'May', carbon: 0.18, readiness: 81 }, { month: 'Jun', carbon: 0.2, readiness: 86 }];
const fmt = (v?: number, d = 1) => v === undefined || !Number.isFinite(v) ? '0' : new Intl.NumberFormat('en-IN', { maximumFractionDigits: d, minimumFractionDigits: d }).format(v);
const int = (v?: number) => v === undefined || !Number.isFinite(v) ? '0' : new Intl.NumberFormat('en-IN', { maximumFractionDigits: 0 }).format(v);
const cn = (...x: Array<string | false | null | undefined>) => x.filter(Boolean).join(' ');

function Card({ children, className = '' }: { children: React.ReactNode; className?: string }) { return <div className={cn('rounded-3xl border border-slate-200 bg-white shadow-sm', className)}>{children}</div>; }
function CardContent({ children, className = '' }: { children: React.ReactNode; className?: string }) { return <div className={cn('p-5', className)}>{children}</div>; }
function Badge({ children, tone = 'slate' }: { children: React.ReactNode; tone?: 'slate'|'green'|'blue'|'amber'|'red'|'purple'|'dark' }) { const tones = { slate:'bg-slate-100 text-slate-700 ring-slate-200', green:'bg-emerald-50 text-emerald-700 ring-emerald-200', blue:'bg-blue-50 text-blue-700 ring-blue-200', amber:'bg-amber-50 text-amber-700 ring-amber-200', red:'bg-red-50 text-red-700 ring-red-200', purple:'bg-purple-50 text-purple-700 ring-purple-200', dark:'bg-slate-900 text-white ring-slate-800' }; return <span className={cn('inline-flex items-center rounded-full px-2.5 py-1 text-xs font-semibold ring-1', tones[tone])}>{children}</span>; }
function MetricCard({ title, value, subtitle, icon: Icon, tone = 'green', foot, status }: any) { const styles: any = { green:'text-emerald-700 bg-emerald-50', blue:'text-blue-700 bg-blue-50', amber:'text-amber-700 bg-amber-50', purple:'text-purple-700 bg-purple-50', slate:'text-slate-700 bg-slate-100' }; return <Card className="overflow-hidden"><CardContent className="p-0"><div className="p-5"><div className="flex items-start justify-between gap-4"><div><p className="text-sm font-medium text-slate-500">{title}</p><p className="mt-2 text-3xl font-semibold tracking-tight text-slate-950">{value}</p><p className="mt-1 text-sm leading-5 text-slate-500">{subtitle}</p></div><div className={cn('rounded-2xl p-3', styles[tone])}><Icon className="h-5 w-5" /></div></div></div><div className="flex items-center justify-between gap-3 border-t border-slate-100 bg-slate-50/80 px-5 py-3 text-xs text-slate-500"><span>{foot}</span><Badge tone={status.tone}>{status.label}</Badge></div></CardContent></Card>; }
function SectionHeader({ eyebrow, title, description }: any) { return <div className="mb-6"><p className="text-xs font-bold uppercase tracking-[0.2em] text-emerald-700">{eyebrow}</p><h2 className="mt-2 max-w-4xl text-2xl font-semibold tracking-tight text-slate-950 lg:text-3xl">{title}</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-600">{description}</p></div>; }
function SelectField({ label, value, onChange, options }: { label:string; value:string; onChange:(v:string)=>void; options:string[] }) { return <label className="block"><span className="text-xs font-semibold text-slate-600">{label}</span><select value={value} onChange={e=>onChange(e.target.value)} className="mt-1 w-full rounded-2xl border border-slate-200 bg-white px-3 py-2.5 text-sm text-slate-900 outline-none transition focus:border-emerald-500 focus:ring-4 focus:ring-emerald-100">{options.map(o=><option key={o} value={o}>{o}</option>)}</select></label>; }
function TextField({ label, value, onChange, type = 'text' }: { label:string; value:string|number; onChange:(v:string)=>void; type?:string }) { return <label className="block"><span className="text-xs font-semibold text-slate-600">{label}</span><input type={type} value={value} onChange={e=>onChange(e.target.value)} className="mt-1 w-full rounded-2xl border border-slate-200 bg-white px-3 py-2.5 text-sm text-slate-900 outline-none transition focus:border-emerald-500 focus:ring-4 focus:ring-emerald-100" /></label>; }
function HeroSignal({ title, body, icon: Icon }: any) { return <div className="rounded-3xl border border-white/10 bg-white/8 p-4 backdrop-blur"><div className="mb-3 flex h-10 w-10 items-center justify-center rounded-2xl bg-white/10 text-emerald-200"><Icon className="h-5 w-5" /></div><p className="text-sm font-semibold text-white">{title}</p><p className="mt-1 text-xs leading-5 text-emerald-50/70">{body}</p></div>; }

export default function RathGreenOpsCloud() {
  const dashboard = selectCeoDashboard(demoSnapshot);
  const narrative = selectMapletreePilotNarrative(demoSnapshot);
  const [activeTab, setActiveTab] = useState('command');
  const [persona, setPersona] = useState('Leadership');
  const [carbonGap, setCarbonGap] = useState(1000);
  const [draftAsset, setDraftAsset] = useState({ zone: 'New Zone', assetType: 'Indoor Plant' as AssetType, species: 'Areca Palm', quantity: 10, health: 'Healthy' as HealthCondition, evidence: 'E0' as EvidenceLevel, reportingRight: 'Confirmed' as ReportingRight });
  const updatedExternalCredits = Math.max(carbonGap - dashboard.internalGreenAssetContributionTco2e, 0);
  const assetMix = useMemo(() => { const groups = new Map<string, number>(); for (const r of demoSnapshot.details.carbon.byAsset) groups.set(r.assetType, (groups.get(r.assetType) ?? 0) + r.eligibleAnnualContributionKgCo2e / 1000); return Array.from(groups.entries()).map(([type, value]) => ({ type, value: Number(value.toFixed(4)) })); }, []);
  const tabs = [{ key:'command', label:'Command', icon:Gauge }, { key:'field', label:'Field', icon:ScanLine }, { key:'register', label:'Register', icon:Layers3 }, { key:'evidence', label:'Evidence Studio', icon:FileCheck2 }, { key:'engine', label:'Engine', icon:Database }, { key:'architecture', label:'Architecture', icon:Network }];
  const personaCopy: Record<string,string> = { Leadership:'Asset value, external credit planning, water reuse, and campus sustainability story.', ESG:'Evidence levels, claim-safe reporting, data readiness, and quarterly evidence pack.', Workplace:'Health, tickets, replacements, SLA, zone risks, and site action visibility.', IFM:'Retention, tender differentiation, operating proof, and managed-service upsell.' };
  return <div className="min-h-screen bg-[#f6f7f4] text-slate-950"><div className="relative overflow-hidden bg-slate-950"><div className="absolute left-1/2 top-[-12rem] h-[34rem] w-[34rem] -translate-x-1/2 rounded-full bg-emerald-400/20 blur-3xl" /><header className="relative mx-auto flex max-w-7xl flex-col gap-6 px-5 py-6 lg:flex-row lg:items-center lg:justify-between"><div className="flex items-center gap-3"><div className="rounded-3xl bg-white/10 p-3 text-emerald-200 ring-1 ring-white/10"><Leaf className="h-7 w-7" /></div><div><h1 className="text-xl font-semibold tracking-tight text-white">RATH GreenOps Cloud</h1><p className="text-sm text-emerald-50/60">Green asset intelligence for carbon, water, biodiversity, ITSM evidence, and ESG reporting.</p></div></div><div className="flex flex-wrap items-center gap-2"><Badge tone="dark">Stage 2 Product Prototype</Badge><Badge tone="green">{METHOD_VERSION}</Badge><Badge tone="amber">Sample Data</Badge></div></header><section className="relative mx-auto grid max-w-7xl gap-6 px-5 pb-8 lg:grid-cols-[1.22fr_0.78fr] lg:pb-12"><div className="rounded-[2rem] border border-white/10 bg-white/[0.06] p-6 shadow-2xl backdrop-blur-xl lg:p-8"><div className="flex flex-wrap items-center gap-2"><Badge tone="green">Internal contribution, not credit issuance</Badge><Badge tone="blue">Right-to-report aware</Badge><Badge tone="purple">Evidence Studio ready</Badge></div><h2 className="mt-6 max-w-4xl text-4xl font-semibold tracking-tight text-white lg:text-6xl">The operating system for measurable green assets.</h2><p className="mt-5 max-w-3xl text-base leading-7 text-emerald-50/70">Onescape captures field truth. RATH calculates green asset value. Evidence Studio converts it into claim-safe ESG material.</p><button onClick={()=>setActiveTab('command')} className="mt-7 inline-flex items-center justify-center gap-2 rounded-2xl bg-emerald-300 px-5 py-3 text-sm font-bold text-emerald-950">Open Command Center <ArrowRight className="h-4 w-4" /></button></div><div className="grid gap-3 sm:grid-cols-3 lg:grid-cols-1"><HeroSignal icon={Building2} title="Asset owner value" body="Convert landscape cost into measurable campus sustainability value." /><HeroSignal icon={ShieldCheck} title="IFM advantage" body="Retention, QBR, tender differentiation, and premium managed-service layer." /><HeroSignal icon={Lock} title="Claim safety" body="Evidence level, reporting right, method version, and guardrails." /></div></section></div><main className="mx-auto max-w-7xl px-5 py-6"><nav className="sticky top-0 z-20 mb-6 flex gap-2 overflow-x-auto rounded-3xl border border-slate-200/80 bg-white/85 p-2 shadow-sm backdrop-blur-xl">{tabs.map(tab=>{const Icon=tab.icon;return <button key={tab.key} onClick={()=>setActiveTab(tab.key)} className={cn('flex min-w-fit items-center gap-2 rounded-2xl px-4 py-2.5 text-sm font-semibold transition', activeTab===tab.key?'bg-slate-950 text-white shadow-sm':'text-slate-600 hover:bg-slate-100 hover:text-slate-950')}><Icon className="h-4 w-4" />{tab.label}</button>})}</nav>{activeTab==='command'&&<section><div className="mb-6 grid gap-4 lg:grid-cols-[0.72fr_1.28fr]"><Card><CardContent><p className="text-xs font-bold uppercase tracking-[0.18em] text-emerald-700">Role-aware interface</p><h3 className="mt-2 text-xl font-semibold tracking-tight text-slate-950">One system. Four business views.</h3><div className="mt-5 grid grid-cols-2 gap-2">{Object.keys(personaCopy).map(item=><button key={item} onClick={()=>setPersona(item)} className={cn('rounded-2xl px-3 py-3 text-left text-sm font-semibold transition', persona===item?'bg-emerald-950 text-white':'bg-slate-50 text-slate-700 hover:bg-slate-100')}>{item}</button>)}</div><p className="mt-4 rounded-2xl bg-slate-50 p-4 text-sm leading-6 text-slate-600">{personaCopy[persona]}</p></CardContent></Card><Card><CardContent><p className="text-xs font-bold uppercase tracking-[0.18em] text-emerald-700">Executive Command Center</p><h2 className="mt-2 text-2xl font-semibold tracking-tight text-slate-950">Sample Campus — Bengaluru</h2><p className="mt-1 text-sm text-slate-500">Sample values are simulated. Backend calculation engine is live.</p><div className="mt-4 flex flex-wrap gap-2"><Badge tone="amber">{dashboard.evidenceLevel} Internal Model</Badge><Badge tone="blue">Right-to-report confirmed</Badge><Badge tone="green">Not a carbon credit</Badge></div></CardContent></Card></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><MetricCard title="Eligible Internal Contribution" value={`${fmt(dashboard.internalGreenAssetContributionTco2e,3)} tCO₂e/yr`} subtitle="Confidence-adjusted green asset contribution" icon={Trees} tone="green" foot="Indoor + outdoor assets" status={{label:'Modelled',tone:'amber'}} /><MetricCard title="Credit Dependency Planning" value={`${fmt(updatedExternalCredits,1)} credits`} subtitle={`From ${int(carbonGap)} tCO₂e target gap`} icon={ShieldCheck} tone="blue" foot="Subject to auditor acceptance" status={{label:'Planning',tone:'blue'}} /><MetricCard title="Water Reuse + Saving" value={`${int(dashboard.waterReusedLitres + dashboard.freshwaterAvoidedLitres)} L`} subtitle={`${int(dashboard.waterReusedLitres)} L reused · ${int(dashboard.freshwaterAvoidedLitres)} L avoided`} icon={Droplets} tone="blue" foot="Meter / flow-test ready" status={{label:'Measured',tone:'green'}} /><MetricCard title="LEAP Readiness" value={`${fmt(dashboard.leapReadinessScore100,0)}/100`} subtitle={`${dashboard.leapReadinessStatus} nature baseline`} icon={BadgeCheck} tone="purple" foot={`Biodiversity ${fmt(dashboard.biodiversityReadinessScore10,1)}/10`} status={{label:'Nature',tone:'purple'}} /></div><div className="mt-5 grid gap-5 lg:grid-cols-[1.35fr_0.65fr]"><Card><CardContent><h3 className="text-base font-semibold text-slate-950">Contribution and readiness trend</h3><div className="mt-5 h-72"><ResponsiveContainer width="100%" height="100%"><AreaChart data={timelineData}><CartesianGrid strokeDasharray="3 3" stroke="#e2e8f0" /><XAxis dataKey="month" tickLine={false} axisLine={false} /><YAxis tickLine={false} axisLine={false} /><Tooltip /><Area type="monotone" dataKey="carbon" stroke="#059669" strokeWidth={3} fill="#d1fae5" name="Eligible tCO₂e" /><Area type="monotone" dataKey="readiness" stroke="#2563eb" strokeWidth={2} fill="#dbeafe" name="Readiness score" /></AreaChart></ResponsiveContainer></div></CardContent></Card><Card><CardContent><h3 className="text-base font-semibold text-slate-950">Credit planning simulator</h3><div className="mt-4"><TextField label="Target carbon gap (tCO₂e)" type="number" value={carbonGap} onChange={v=>setCarbonGap(Number(v))} /></div><div className="mt-5 rounded-3xl bg-slate-950 p-5 text-white"><p className="text-xs font-bold uppercase tracking-[0.18em] text-emerald-300">Planning formula</p><p className="mt-3 text-sm leading-6 text-white/70">External planning need = carbon gap − eligible internal green asset contribution.</p><div className="mt-4 flex items-center gap-2 text-sm"><span>{fmt(carbonGap,1)}</span><span>−</span><span>{fmt(dashboard.internalGreenAssetContributionTco2e,3)}</span><span>=</span><span className="font-semibold text-emerald-300">{fmt(updatedExternalCredits,1)}</span></div></div></CardContent></Card></div></section>}{activeTab==='field'&&<section><SectionHeader eyebrow="Field capture" title="Designed for gardeners, not analysts." description="The field team selects practical ranges and conditions. RATH converts those inputs into technical outputs." /><div className="grid gap-5 lg:grid-cols-[0.85fr_1.15fr]"><Card><CardContent><div className="mb-5 flex gap-2"><button className="rounded-2xl border border-slate-200 bg-slate-50 px-3 py-3 text-xs font-bold text-slate-700"><QrCode className="mx-auto mb-1 h-4 w-4" />Scan</button><button className="rounded-2xl border border-slate-200 bg-slate-50 px-3 py-3 text-xs font-bold text-slate-700"><Camera className="mx-auto mb-1 h-4 w-4" />Photo</button><button className="rounded-2xl border border-slate-200 bg-slate-50 px-3 py-3 text-xs font-bold text-slate-700"><Upload className="mx-auto mb-1 h-4 w-4" />Upload</button></div><div className="space-y-4"><TextField label="Zone" value={draftAsset.zone} onChange={v=>setDraftAsset(d=>({...d,zone:v}))} /><SelectField label="Asset Type" value={draftAsset.assetType} onChange={v=>setDraftAsset(d=>({...d,assetType:v as AssetType}))} options={['Indoor Plant','Tree','Palm','Shrub','Hedge','Lawn','Groundcover','Green Wall','Vertical Garden']} /><TextField label="Species / Common Name" value={draftAsset.species} onChange={v=>setDraftAsset(d=>({...d,species:v}))} /><TextField label="Quantity" type="number" value={draftAsset.quantity} onChange={v=>setDraftAsset(d=>({...d,quantity:Number(v)}))} /><SelectField label="Health Condition" value={draftAsset.health} onChange={v=>setDraftAsset(d=>({...d,health:v as HealthCondition}))} options={['Healthy','Average','Stressed','Critical']} /><SelectField label="Evidence Level" value={draftAsset.evidence} onChange={v=>setDraftAsset(d=>({...d,evidence:v as EvidenceLevel}))} options={['E0','E1','E2','E3']} /><SelectField label="Reporting Right" value={draftAsset.reportingRight} onChange={v=>setDraftAsset(d=>({...d,reportingRight:v as ReportingRight}))} options={['Confirmed','Unclear','Not Granted']} /><button className="flex w-full items-center justify-center gap-2 rounded-2xl bg-slate-950 px-4 py-3 text-sm font-bold text-white"><Plus className="h-4 w-4" /> Add Asset</button></div></CardContent></Card><Card><CardContent><h3 className="text-base font-semibold text-slate-950">Field work queue</h3><div className="mt-4 grid gap-3 md:grid-cols-2">{[['Scan pending assets','17 QR tags pending field confirmation',QrCode],['Upload closure photos','2 tickets need after-service images',Camera],['Water reading due','STP mainline monthly reading due',Droplets],['Health inspection','Work Floor A low-light cluster',Leaf]].map(([title,body,Icon]:any)=><div key={title} className="rounded-3xl border border-slate-100 bg-slate-50 p-4"><div className="mb-3 flex items-center gap-3"><div className="rounded-2xl bg-white p-2 text-slate-700"><Icon className="h-4 w-4" /></div><Badge tone="amber">{title}</Badge></div><p className="text-sm leading-6 text-slate-600">{body}</p></div>)}</div></CardContent></Card></div></section>}{activeTab==='register'&&<section><SectionHeader eyebrow="Green Asset System of Record" title="Every asset ties back to an ID." description="This is the dependency layer and audit spine." /><Card><CardContent><div className="overflow-hidden rounded-3xl border border-slate-200"><table className="min-w-full divide-y divide-slate-200 text-sm"><thead className="bg-slate-50 text-left text-xs font-bold uppercase tracking-wide text-slate-500"><tr><th className="px-4 py-3">Asset ID</th><th className="px-4 py-3">Type</th><th className="px-4 py-3">Species</th><th className="px-4 py-3">Evidence</th><th className="px-4 py-3">Reporting</th><th className="px-4 py-3">Health</th></tr></thead><tbody className="divide-y divide-slate-100 bg-white">{demoAssets.map(asset=><tr key={asset.assetId}><td className="px-4 py-3 font-semibold">{asset.assetId}</td><td className="px-4 py-3">{asset.assetType}</td><td className="px-4 py-3">{asset.speciesCommonName}</td><td className="px-4 py-3"><Badge tone="amber">{asset.evidenceLevel}</Badge></td><td className="px-4 py-3"><Badge tone="green">{asset.reportingRight}</Badge></td><td className="px-4 py-3"><Badge tone={asset.healthCondition==='Healthy'?'green':'amber'}>{asset.healthCondition}</Badge></td></tr>)}</tbody></table></div></CardContent></Card></section>}{activeTab==='evidence'&&<section><SectionHeader eyebrow="Evidence Studio" title="ESG homework with claim-control guardrails." description="Missing data checks, summary drafts, claim checks, and improvement recommendations." /><div className="grid gap-5 lg:grid-cols-[1.25fr_0.75fr]"><Card><CardContent><div className="mb-5 flex items-center gap-3"><div className="rounded-2xl bg-purple-50 p-3 text-purple-700"><Bot className="h-5 w-5" /></div><div><h3 className="text-base font-semibold text-slate-950">Readiness check</h3><p className="text-sm text-slate-500">Generated from site records.</p></div></div><div className="space-y-3">{narrative.map(finding=><div key={finding} className="flex items-start gap-3 rounded-3xl bg-slate-50 p-4">{finding.includes('not certified')?<AlertTriangle className="mt-0.5 h-5 w-5 text-amber-600" />:<CheckCircle2 className="mt-0.5 h-5 w-5 text-emerald-600" />}<p className="text-sm leading-6 text-slate-700">{finding}</p></div>)}</div><div className="mt-5 grid gap-3 md:grid-cols-4">{[[Sparkles,'Generate Summary'],[ShieldCheck,'Check Claims'],[Zap,'Recommend'],[Download,'Export Pack']].map(([Icon,label]:any)=><button key={label} className="flex items-center justify-center gap-2 rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm font-bold text-slate-700"><Icon className="h-4 w-4" /> {label}</button>)}</div></CardContent></Card><Card><CardContent><h3 className="text-base font-semibold text-slate-950">Auto-generated evidence note</h3><p className="mt-3 text-sm leading-6 text-slate-600">During the reporting period, the site maintained {int(demoSnapshot.totalGreenAssets)} registered green assets. Based on {METHOD_VERSION}, the site recorded an eligible internal green asset contribution estimate of {fmt(dashboard.internalGreenAssetContributionTco2e,3)} tCO₂e/year at {dashboard.evidenceLevel} evidence level. This is an internal planning estimate and not a certified carbon credit.</p><div className="mt-4 rounded-3xl border border-amber-200 bg-amber-50 p-4"><div className="flex items-start gap-3"><ShieldAlert className="mt-0.5 h-5 w-5 text-amber-700" /><p className="text-sm leading-6 text-amber-900">Prohibited: “RATH generated carbon credits.” Allowed: “RATH estimated internal green asset contribution, subject to validation and auditor acceptance.”</p></div></div></CardContent></Card></div></section>}{activeTab==='engine'&&<section><SectionHeader eyebrow="Methodology engine" title="The master sheet becomes a controlled calculation service." description="Production separates raw inputs, calculation snapshots, report claims, and methodology versions." /><div className="grid gap-5 lg:grid-cols-[1.1fr_0.9fr]"><Card><CardContent><h3 className="text-base font-semibold text-slate-950">Asset contribution by class</h3><div className="mt-5 h-72"><ResponsiveContainer width="100%" height="100%"><BarChart data={assetMix}><CartesianGrid strokeDasharray="3 3" stroke="#e2e8f0" /><XAxis dataKey="type" /><YAxis /><Tooltip /><Bar dataKey="value" name="Eligible tCO₂e" fill="#059669" radius={[10,10,0,0]} /></BarChart></ResponsiveContainer></div></CardContent></Card><Card><CardContent><h3 className="text-base font-semibold text-slate-950">Evidence ladder</h3><div className="mt-5 space-y-3">{[['E0','Internal Model','0.25'],['E1','Expert Reviewed','0.55'],['E2','Third-party Reviewed','0.80'],['E3','Auditor Accepted','1.00']].map(([code,label,factor])=><div key={code} className="flex items-center justify-between rounded-3xl border border-slate-100 p-4"><div><p className="text-sm font-semibold text-slate-950">{code} — {label}</p><p className="text-xs text-slate-500">Confidence factor: {factor}</p></div><Badge tone={code==='E0'?'amber':code==='E3'?'green':'blue'}>{code}</Badge></div>)}</div></CardContent></Card></div></section>}{activeTab==='architecture'&&<section><SectionHeader eyebrow="Backend + frontend architecture" title="Field truth → Green Asset Graph → Methodology Engine → Evidence Studio → Role dashboards." description="Built for low input burden, claim safety, future modules, and enterprise reporting dependency." /><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-5">{[[MapPin,'Baseline','RATH maps green assets, zones, water sources, ownership, and reporting rights.'],[QrCode,'Capture','Field team scans QR, uploads photo, updates health, and logs events.'],[Database,'Calculate','Methodology engine creates versioned carbon, water, biodiversity, and evidence snapshots.'],[Bot,'Generate','Evidence Studio drafts summaries, checks claims, identifies missing data, and recommends actions.'],[FileText,'Report','Client receives dashboards, evidence pack, and improvement roadmap.']].map(([Icon,title,body]:any,i)=><Card key={title}><CardContent><div className="mb-4 flex items-center justify-between"><div className="rounded-2xl bg-emerald-50 p-3 text-emerald-700"><Icon className="h-5 w-5" /></div><span className="text-sm font-bold text-slate-300">0{i+1}</span></div><h3 className="text-base font-semibold text-slate-950">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-600">{body}</p></CardContent></Card>)}</div></section>}</main></div>;
}
